NOVA dashboard rollback

Replace ONLY:
D:\Nova-Sports-AI-v3\frontend\app\page.tsx

with:
frontend/app/page.tsx

IMPORTANT:
- Do NOT replace dashboard.css with the previously generated dashboard.css.
- Keep your existing dashboard.css, because the goal is to restore the original dashboard UI.
- This rollback restores the dashboard page source from the user's previously uploaded page(1).tsx.
