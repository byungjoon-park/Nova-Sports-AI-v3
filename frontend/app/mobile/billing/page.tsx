"use client";
/* eslint-disable react-hooks/set-state-in-effect */
import { useEffect, useState } from "react";
import { useRouter } from "next/navigation";
import { getCurrentUser } from "../../../lib/nova-auth";
import "../mobile.css";
export default function MobileBillingPage(){const router=useRouter();const [user,setUser]=useState<ReturnType<typeof getCurrentUser>>(null);useEffect(()=>{const u=getCurrentUser();if(!u){router.replace("/mobile/login");return;}if(u.role!=="athlete"&&u.role!=="parent"){router.replace("/mobile");return;}setUser(u)},[router]);if(!user)return null;return <main className="mobile-page theme-ivory"><header className="mobile-subheader"><div className="mobile-desktop-header"><div className="mobile-desktop-header-left"><button onClick={()=>router.push("/mobile")} aria-label="뒤로">←</button><div className="mobile-desktop-brand"><strong>N O V A</strong><span>AI SPORTS PERFORMANCE PLATFORM</span></div></div><div className="mobile-ai-status"><i></i> AI 시스템 준비</div></div><div className="mobile-page-title"><span className="mobile-eyebrow">NOVA SPORTS AI · MOBILE</span><h1>결제</h1><p>Premium 구독과 이용 권한을 관리합니다.</p></div></header><section className="mobile-content-card"><div className="mobile-card-heading"><span>PREMIUM</span><strong>{user.role==="parent"?"학부모 이용 권한":"개인 Premium"}</strong></div>{user.role==="parent"?<><p>자녀의 Premium에 포함된 학부모 계정입니다. 별도 결제가 필요하지 않습니다.</p><button className="mobile-action-button" onClick={()=>router.push("/mobile")}>대시보드</button></>:<><p>선수 개인 구독을 결제하면 연결한 학부모 계정도 추가 결제 없이 함께 사용할 수 있습니다.</p><button className="mobile-action-button" onClick={()=>router.push("/billing")}>결제 페이지 열기 ›</button></>}</section></main>}
